{
  "name": "jira-time-tracker",
  "version": "1.0.0",
  "description": "Time tracker tool for Jira",
  "dependencies": {
    "menubar": "*"
  },
  "devDependencies": {
    "electron-packager": "^5.1.0",
    "electron-prebuilt": "^0.36.0"
  },
  "main": "src/main.js",
  "scripts": {
    "build": "electron-packager . 'Time Tracker' --platform=darwin --arch=x64 --version=0.36.0 --icon=jira.icns",
    "start": "electron ."
  }
}
